ProjectRoot/
    README.md (project overview, setup instructions)
    requirements.txt (Python package dependencies)
    
    data/
        README.md (explain dataset structure, file locations)
        (No need to submit raw data files as they may be large)
    
    src/
        main.py (main entry point - must be runnable)
        preprocessing.py (data cleaning and preparation)
        features.py (feature engineering)
        train.py (model training)
        evaluate.py (model evaluation and metrics)
    
    notebooks/
        (Jupyter notebooks, optional; if used for exploration)
    
    results/
        figures/ (all visualizations: plots, charts)
        metrics/ (saved metrics: JSON, CSV, or text files)